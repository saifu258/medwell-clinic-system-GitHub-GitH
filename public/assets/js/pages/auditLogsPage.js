import { apiGet } from "../api.js";
import { escapeHtml, formatDateTime } from "../formatters.js";
import { pageHeader, table, loadingState, errorState, wireCommonActions } from "../components/ui.js";
export async function render(root) { const refresh = () => render(root); root.innerHTML = pageHeader("Audit Log") + loadingState(); try { const rows = await apiGet("/audit-logs"); root.innerHTML = pageHeader("Audit Log", "บันทึกเหตุการณ์สำคัญโดยไม่เก็บรหัสผ่าน Token หรือเวชระเบียนฉบับเต็ม") + `<section class="card">${table(["วันเวลา", "ผู้ใช้งาน", "เหตุการณ์", "โมดูล", "รายการ", "ผลลัพธ์"], rows, (r) => [formatDateTime(r.occurredAt), escapeHtml(r.userName || r.userUid), escapeHtml(r.action), escapeHtml(r.module), escapeHtml(r.recordId), String(r.success) === "true" ? '<span class="badge success">สำเร็จ</span>' : '<span class="badge danger">ไม่สำเร็จ</span>'], "ยังไม่มี Audit Log")}</section>`; } catch (error) { root.innerHTML = pageHeader("Audit Log") + errorState(error.message); } wireCommonActions(root, refresh); }
