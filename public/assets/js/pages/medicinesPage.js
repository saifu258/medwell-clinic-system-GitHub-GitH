import { apiGet, apiPost } from "../api.js";
import { escapeHtml, formatMoney } from "../formatters.js";
import { pageHeader, table, field, loadingState, errorState, wireCommonActions } from "../components/ui.js";
import { formToObject } from "../validators.js";
import { toast, setButtonLoading } from "../notifications.js";
export async function render(root) { const refresh = () => render(root); root.innerHTML = pageHeader("ทะเบียนยา") + loadingState(); try { const medicines = await apiGet("/medicines"); root.innerHTML = pageHeader("ทะเบียนยา", `${medicines.length} รายการ`, `<button class="btn btn-primary" id="add-medicine"><i data-lucide="plus"></i> เพิ่มยา</button>`) + `<section class="card" id="medicine-form" hidden><form class="card-body form-grid">${field({ name: "medicineCode", label: "รหัสยา", required: true })}${field({ name: "genericName", label: "ชื่อสามัญ", required: true })}${field({ name: "tradeName", label: "ชื่อการค้า" })}${field({ name: "strength", label: "ความแรง" })}${field({ name: "dosageForm", label: "รูปแบบยา" })}${field({ name: "unit", label: "หน่วย", required: true })}${field({ name: "minimumStock", label: "สต็อกขั้นต่ำ", type: "number", value: 10 })}${field({ name: "sellingPrice", label: "ราคาขาย", type: "number" })}<div class="span-2 text-right"><button class="btn btn-primary" type="submit">บันทึก</button></div></form></section><section class="card mt-3">${table(["รหัส", "ชื่อยา", "ความแรง", "หน่วย", "ขั้นต่ำ", "ราคาขาย", "สถานะ"], medicines, (m) => [escapeHtml(m.medicineCode), escapeHtml(m.genericName || m.tradeName), escapeHtml(m.strength), escapeHtml(m.unit), Number(m.minimumStock || 0), formatMoney(m.sellingPrice), String(m.active) === "false" ? '<span class="badge danger">ปิด</span>' : '<span class="badge success">ใช้งาน</span>'], "ยังไม่มีรายการยา")}</section>`; root.querySelector("#add-medicine").addEventListener("click", () => { root.querySelector("#medicine-form").hidden = false; }); const form = root.querySelector("#medicine-form form"); form.addEventListener("submit", async (event) => { event.preventDefault(); const button = form.querySelector("button"); setButtonLoading(button, true); try { await apiPost("/medicines", { ...formToObject(form), active: true }); toast("เพิ่มยาแล้ว"); refresh(); } catch (error) { toast(error.message, "error"); } finally { setButtonLoading(button, false); } }); } catch (error) { root.innerHTML = pageHeader("ทะเบียนยา") + errorState(error.message); } wireCommonActions(root, refresh); }
