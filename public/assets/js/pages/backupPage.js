import { apiGet } from "../api.js";
import { pageHeader, wireCommonActions } from "../components/ui.js";
import { toast, confirmDialog } from "../notifications.js";
const sheets = ["Settings", "Users", "Patients", "Appointments", "Queues", "Screenings", "Visits", "VisitAddendums", "Diagnoses", "Prescriptions", "PrescriptionItems", "Medicines", "StockLots", "StockMovements", "Services", "Invoices", "InvoiceItems", "Payments", "AuditLogs", "Counters"];
export async function render(root) { const refresh = () => render(root); root.innerHTML = pageHeader("สำรองและส่งออกข้อมูล", "สำหรับผู้ดูแลระบบเท่านั้น") + `<section class="card"><div class="card-body"><div class="alert alert-warning">ไฟล์สำรองมีข้อมูลส่วนบุคคลและข้อมูลสุขภาพ โปรดจัดเก็บในพื้นที่เข้ารหัสและจำกัดสิทธิ์</div><div class="quick-grid mt-3">${sheets.map((sheet) => `<button class="quick-action" data-sheet="${sheet}"><i data-lucide="file-down"></i><strong>${sheet}</strong><span>ส่งออก JSON</span></button>`).join("")}</div></div></section>`; root.querySelectorAll("[data-sheet]").forEach((button) => button.addEventListener("click", async () => { const allowed = await confirmDialog(`ส่งออก ${button.dataset.sheet}?`, "ระบบจะบันทึกเหตุการณ์นี้ใน Audit Log"); if (!allowed) return; try { const data = await apiGet(`/backup/${button.dataset.sheet}`); const link = document.createElement("a"); link.href = URL.createObjectURL(new Blob([JSON.stringify(data, null, 2)], { type: "application/json" })); link.download = `medwell-${button.dataset.sheet}-${new Date().toISOString().slice(0, 10)}.json`; link.click(); URL.revokeObjectURL(link.href); toast("ดาวน์โหลดไฟล์สำรองแล้ว"); } catch (error) { toast(error.message, "error"); } })); wireCommonActions(root, refresh); }
