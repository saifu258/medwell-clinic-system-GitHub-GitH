import { apiGet, apiPost } from "../api.js";
import { pageHeader, field, errorState, wireCommonActions } from "../components/ui.js";
import { setButtonLoading, toast, confirmDialog } from "../notifications.js";
export async function render(root) { const refresh = () => render(root); root.innerHTML = pageHeader("จ่ายยา", "ตรวจสอบใบสั่งยาและตัดสต็อกตาม FEFO") + `<section class="card"><form class="card-body form-grid" id="dispense-form">${field({ name: "prescriptionId", label: "Prescription ID", required: true })}<div class="form-group"><label>&nbsp;</label><button class="btn btn-primary" type="submit"><i data-lucide="package-check"></i> ตรวจสอบและจ่ายยา</button></div></form><div class="alert alert-info" style="margin:0 20px 20px">ระบบเลือก Lot ที่หมดอายุก่อนโดยอัตโนมัติ และจะไม่อนุญาตให้จ่ายยาเกินสต็อกหรือใช้ Lot หมดอายุ</div></section>`; const form = root.querySelector("#dispense-form"); form.addEventListener("submit", async (event) => { event.preventDefault(); const ok = await confirmDialog("ยืนยันการจ่ายยา", "ระบบจะตัดสต็อกทันที"); if (!ok) return; const button = form.querySelector("button[type=submit]"); setButtonLoading(button, true); try { const rx = await apiGet(`/prescriptions/${form.prescriptionId.value}`); await apiPost(`/prescriptions/${rx.prescriptionId}/dispense`, { idempotencyKey: crypto.randomUUID() }); toast("จ่ายยาและตัดสต็อกแล้ว"); form.reset(); } catch (error) { toast(error.message, "error"); } finally { setButtonLoading(button, false); } }); wireCommonActions(root, refresh); }
