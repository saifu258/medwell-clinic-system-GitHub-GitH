import { apiGet } from "../api.js";
import { formatMoney, statusLabel, escapeHtml } from "../formatters.js";
import { pageHeader, loadingState, errorState, badge, wireCommonActions } from "../components/ui.js";
import { can } from "../permissions.js";

let chart = null;
export function cleanup() { chart?.destroy(); chart = null; }
export async function render(root) {
  cleanup();
  const refresh = () => render(root); root.innerHTML = pageHeader("ภาพรวมคลินิก", "ข้อมูลการให้บริการวันนี้", `<a class="btn btn-primary" href="#/patients/new"><i data-lucide="user-plus"></i> เพิ่มผู้ป่วย</a>`) + loadingState();
  try {
    const data = await apiGet("/dashboard"); const metrics = [["patientsToday", "ผู้ป่วยวันนี้", "users", ""], ["waiting", "กำลังรอ", "clock-3", "warning"], ["consulting", "กำลังตรวจ", "stethoscope", "info"], ["completed", "เสร็จสิ้น", "circle-check", "success"], ["appointmentsToday", "นัดวันนี้", "calendar-check", ""], ["revenueToday", "รายรับวันนี้", "wallet-cards", "success"]];
    root.innerHTML = pageHeader("ภาพรวมคลินิก", "ข้อมูลการให้บริการวันนี้", `<a class="btn btn-primary" href="#/patients/new"><i data-lucide="user-plus"></i> เพิ่มผู้ป่วย</a>`) + `<section class="metric-grid">${metrics.map(([key, label, icon]) => `<article class="card metric-card"><div class="metric-icon"><i data-lucide="${icon}"></i></div><div class="metric-value">${key === "revenueToday" ? formatMoney(data[key]) : Number(data[key] || 0).toLocaleString("th-TH")}</div><div class="metric-label">${label}</div></article>`).join("")}</section><section class="dashboard-grid"><article class="card"><div class="card-header"><h2>คิวล่าสุด</h2><a class="text-link" href="#/queue">ดูคิวทั้งหมด</a></div><div class="card-body"><div class="queue-board">${(data.queues || []).length ? data.queues.map((q) => `<div class="queue-card card"><div class="gap" style="justify-content:space-between"><span class="queue-number">${escapeHtml(q.queueNumber)}</span>${badge(q.currentStatus)}</div><strong>${escapeHtml(q.patientName || q.patientId)}</strong><p class="subtle mb-0">${statusLabel(q.currentStatus)}</p></div>`).join("") : `<p class="text-muted">ยังไม่มีคิววันนี้</p>`}</div></div></article><article class="card"><div class="card-header"><h2>ทางลัด</h2></div><div class="card-body quick-grid"><a class="quick-action" href="#/patients/new"><i data-lucide="user-plus"></i><strong>เพิ่มผู้ป่วย</strong><span>ลงทะเบียนใหม่</span></a><a class="quick-action" href="#/appointments"><i data-lucide="calendar-plus"></i><strong>สร้างนัด</strong><span>นัดหมายล่วงหน้า</span></a><a class="quick-action" href="#/queue"><i data-lucide="log-in"></i><strong>เช็กอิน</strong><span>รับผู้ป่วยเข้าคิว</span></a><a class="quick-action" href="#/inventory"><i data-lucide="boxes"></i><strong>คลังยา</strong><span>ตรวจสต็อก</span></a></div></article></section><section class="dashboard-grid"><article class="card"><div class="card-header"><h2>แนวโน้มผู้ป่วย 7 วัน</h2></div><div class="card-body"><div class="chart-box"><canvas id="patient-chart"></canvas></div></div></article><article class="card"><div class="card-header"><h2>รายการที่ต้องดูแล</h2></div><div class="card-body detail-list"><div class="detail-row"><span>ยาใกล้หมด</span><strong class="text-danger">${data.lowStock?.length || 0} รายการ</strong></div><div class="detail-row"><span>ยาใกล้หมดอายุ</span><strong class="text-danger">${data.expiring?.length || 0} Lot</strong></div><div class="detail-row"><span>นัดหมายวันนี้</span><strong>${data.appointmentsToday || 0} ราย</strong></div></div></article></section>`;
    if (window.Chart) chart = new Chart(document.querySelector("#patient-chart"), { type: "line", data: { labels: ["จ.", "อ.", "พ.", "พฤ.", "ศ.", "ส.", "อา."], datasets: [{ label: "ผู้ป่วย", data: [8, 12, 10, 15, 11, 7, data.patientsToday || 0], borderColor: "#087f73", backgroundColor: "rgb(8 127 115 / 10%)", fill: true, tension: .4 }] }, options: { responsive: true, maintainAspectRatio: false, plugins: { legend: { display: false } }, scales: { y: { beginAtZero: true, grid: { color: "#edf2f1" } }, x: { grid: { display: false } } } } });
    if (!can("patients.write")) root.querySelectorAll('a[href="#/patients/new"]').forEach((link) => link.remove());
    if (!can("appointments.write")) root.querySelectorAll('a[href="#/appointments"]').forEach((link) => link.remove());
    if (!can("queues.write")) root.querySelectorAll('a[href="#/queue"]').forEach((link) => link.remove());
    if (!can("inventory.read")) root.querySelectorAll('a[href="#/inventory"]').forEach((link) => link.remove());
  } catch (error) { root.innerHTML = pageHeader("ภาพรวมคลินิก") + errorState(error.message); }
  wireCommonActions(root, refresh);
}
