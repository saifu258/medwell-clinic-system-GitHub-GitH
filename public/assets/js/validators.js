export function validateCitizenId(value) { if (!value) return true; if (!/^\d{13}$/.test(value)) return false; let sum = 0; for (let i = 0; i < 12; i += 1) sum += Number(value[i]) * (13 - i); return (11 - (sum % 11)) % 10 === Number(value[12]); }
export function formToObject(form) { const data = Object.fromEntries(new FormData(form)); for (const checkbox of form.querySelectorAll('input[type="checkbox"]')) data[checkbox.name] = checkbox.checked; return data; }
export function validateForm(form) { let valid = true; for (const field of form.querySelectorAll("[required]")) { const bad = !String(field.value).trim(); field.setAttribute("aria-invalid", String(bad)); if (bad) valid = false; } return valid; }
export const calculateAge = (date) => { if (!date) return ""; const birth = new Date(date); const today = new Date(); let age = today.getFullYear() - birth.getFullYear(); if (today < new Date(today.getFullYear(), birth.getMonth(), birth.getDate())) age -= 1; return age; };
